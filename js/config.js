/* config.js — 填入你的 LIFF ID 和 Supabase 設定 */
const APP_CONFIG = {
  // LINE LIFF ID — 從 LINE Developers Console 取得
  LIFF_ID: 'YOUR_LIFF_ID_HERE',

  // Supabase 設定 — 從 Supabase Console 取得
  SUPABASE_URL: 'YOUR_SUPABASE_URL_HERE',
  SUPABASE_ANON_KEY: 'YOUR_SUPABASE_ANON_KEY_HERE',

  // 房間 ID（所有成員共用同一個 room）
  ROOM_ID: 'sweet-home-001',

  // Demo 模式（true = 不連後端，用假資料展示 UI）
  DEMO_MODE: true,
};
